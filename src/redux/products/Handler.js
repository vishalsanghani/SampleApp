import React from 'react';
import {Modules} from "./Actions";

export default {
    [Modules.GET_BANNER_ADS]: ({dispatch, payload, appState}) => {
        console.log("-------GET_BANNER_ADS---H----")
    }
}
